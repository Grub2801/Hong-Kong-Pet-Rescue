app.controller('SignUpCtrl', ['$scope', '$auth', '$location', function($scope, $auth, $location){
  console.log("SignUpCtrl")
  $scope.signupUser = function () {
    $auth.submitRegistration($scope.registrationForm.then(function(resp) {
      // handle success response
      console.log(resp);
      // redirect back to root when registration succesfull
      $location.path('/');
    }).catch(function(resp) {
      // handle error response
      console.log(resp.data.errors);
    }));
  };

  $scope.signupShelter = function () {
    var test = {
      email: "test102@test.com",
      password: "asdfasdf",
      password_confirmation: "asdfasdf",
      location: "hong kong",
      name: "test10",
      address: "test1"
    }

    // var test1 = {
    //   email: "test100@test.com",
    //   password: "12345678",
    //   password_confirmation: "12345678",
    //   location: "hong kong",
    //   name: "test",
    //   address: "test"
    // }

    $scope.registrationForm = test;

    $auth.submitRegistration($scope.registrationForm, {config: "shelter"}).then(function(resp) {
      // redirect back to root when registration succesfull
      $location.path('/');
    }).catch(function(resp) {
      // handle error response
      console.log(resp);
    });
  };
}])
