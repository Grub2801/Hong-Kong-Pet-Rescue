app.factory('FavoriteFactory', FavoriteFactory)

  FavoriteFactory.$inject = ['$resource'];

  var url = "<%= ENV['URL'] %>" || "http://localhost:3000";

  console.log(url);

  function FavoriteFactory($resource) {
   return $resource( url + '/api/favorites/:id',
     {id: '@_id'}
   );

  }